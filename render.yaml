services:
  - type: web
    name: global-messenger-api
    runtime: node
    plan: starter
    region: singapore
    buildCommand: npm install && npm run db:generate && npm run build -w apps/server
    startCommand: node apps/server/dist/index.js
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: global-messenger-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
      - key: WEB_ORIGIN
        sync: false
      - key: UPLOAD_DIR
        value: /var/data/uploads
    disk:
      name: global-messenger-uploads
      mountPath: /var/data
      sizeGB: 1

  - type: web
    name: global-messenger-web
    runtime: static
    buildCommand: npm install && npm run build -w apps/web
    staticPublishPath: apps/web/dist
    envVars:
      - key: VITE_API_URL
        sync: false

databases:
  - name: global-messenger-db
    databaseName: global_messenger
    plan: 0.1c-256mb
    region: singapore
