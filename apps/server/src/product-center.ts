import type { FastifyInstance } from 'fastify';
import type { PrismaClient } from '@prisma/client';
import { z } from 'zod';

type AuthUser = { id: string; username: string };
const secured = (app: FastifyInstance) => ({ preHandler: [app.authenticate] });

export const PRODUCT_FEATURES = [
  { id: 'profile', phase: 1, title: 'Complete Profile Center', status: 'foundation', items: ['profile photo','display name','username','bio','online status','last seen','profile preview','QR/profile sharing','copy username','account ID'] },
  { id: 'chat-info', phase: 1, title: 'Complete Chat Info', status: 'foundation', items: ['contact profile','media','files','links','starred','pinned','search','notifications','disappearing messages','block','report','clear chat','delete chat','group photo','group description','group admin controls','permissions','invite link','leave group'] },
  { id: 'message-tools', phase: 1, title: 'Advanced Message Operations', status: 'foundation', items: ['forward','copy','star/save','pin','quote','multi-select','bulk delete','bulk forward','message info','delivery/read timestamps','retry','download/share','link preview','reply preview','edited indicator','undo destructive actions'] },
  { id: 'organization', phase: 1, title: 'Conversation Organization', status: 'foundation', items: ['favorites','pinned chats','archive','unread filter','groups filter','personal filter','custom folders','mute indicators','unread badges','sorting','recently active','Saved Messages'] },
  { id: 'media', phase: 1, title: 'Media Experience', status: 'foundation', items: ['full-screen image viewer','gallery','video player','audio player','voice messages','waveform recording/playback','document/PDF preview','media grid','files tab','links tab','download/share','compression','upload progress','download progress','cancel/retry/resume','large-file validation','authenticated downloads','cleanup'] },
  { id: 'notifications', phase: 1, title: 'Notification Center', status: 'foundation', items: ['messages','mentions','groups','calls','friend requests','history','per-chat settings','global settings','sounds','desktop controls','notification permission recovery','quiet hours'] },
  { id: 'security', phase: 2, title: 'Privacy & Security Center', status: 'foundation', items: ['active devices','logout other devices','login history','password change','password reset','2FA','passkeys','app PIN','Android biometric lock','screen lock','privacy controls','blocked users','security verification','key/device management','upload security','rate limiting','CORS/CSP/security headers','abuse protection'] },
  { id: 'advanced-messaging', phase: 3, title: 'Advanced Messaging', status: 'planned', items: ['voice notes','polls','scheduled messages','disappearing messages','auto-delete','reminders','live location','location sharing','contacts','events/calendar','link previews','translation','advanced media','audio/video/document players'] },
  { id: 'calls', phase: 4, title: 'Advanced Calls', status: 'foundation', items: ['incoming UI','outgoing UI','accept/reject','history','missed calls','mute','speaker','camera','screen sharing','group calling','call notifications','TURN relay','network interruption recovery','permission denial','background/foreground','audio routing'] },
  { id: 'ai', phase: 5, title: 'AI Workspace', status: 'planned', items: ['rewrite','translation','reply suggestions','conversation summary','AI message search','AI assistant','file understanding','voice transcription','smart notifications','privacy boundary indicator'] },
  { id: 'search', phase: 1, title: 'Universal Search', status: 'foundation', items: ['people','chats','messages','files','photos','links','groups','saved messages','pinned messages','sender/date/type/attachment/link filters'] },
  { id: 'command-center', phase: 1, title: 'Command Center', status: 'foundation', items: ['recent conversations','unread','calls','groups','saved','files','AI assistant','security status','active devices','quick actions','favorites','pending requests','scheduled messages'] },
  { id: 'settings', phase: 1, title: 'Settings Center', status: 'foundation', items: ['account','privacy','security','notifications','appearance','chat','storage','language','about','theme','accent','wallpaper','font size','density','cache management'] },
  { id: 'e2ee', phase: 1, title: 'Multi-device E2EE', status: 'next', items: ['device identity','encrypted key backup','authorized device recovery','device revocation','security verification','historical message recovery','no ciphertext dead-end'] },
  { id: 'reliability', phase: 1, title: 'Offline & Failure Recovery', status: 'next', items: ['offline queue','reconnect','idempotency','duplicate suppression','draft persistence','optimistic reconciliation','upload retry/resume','API timeout/retry','server restart recovery','DB unavailable handling','connection state','ordered event reconciliation'] },
  { id: 'performance', phase: 1, title: 'Performance & Speed', status: 'next', items: ['local cache','optimistic UI','cursor pagination','virtualized message list','debounced search','conversation cache','minimal round trips','DB indexes','query profiling','socket listener deduplication','lazy media thumbnails','batched read receipts','background sync','response compression','Web Vitals','p50/p95/p99 latency telemetry'] },
  { id: 'stories', phase: 2, title: 'Stories & Status', status: 'planned', items: ['photo stories','video stories','text stories','custom audiences','story replies','story reactions','24-hour expiry','story archive','privacy controls'] },
  { id: 'communities', phase: 2, title: 'Communities & Channels', status: 'planned', items: ['communities','announcement channels','topic groups','threads','admin announcements','member permissions','channel moderation','invite links','community search'] },
  { id: 'platform', phase: 3, title: 'Bots, Mini Apps & Developer Platform', status: 'planned', items: ['bot accounts','bot permissions','webhooks','commands','inline actions','mini apps','deep links','developer dashboard','API keys','key rotation','usage limits','audit logs'] },
  { id: 'business', phase: 3, title: 'Business Messaging', status: 'planned', items: ['verified business profiles','business hours','catalog messages','support bots','shared inbox','agent assignment','labels','customer notes','SLA timers','templates','analytics','consent controls'] },
  { id: 'moderation', phase: 2, title: 'Trust, Safety & Abuse Prevention', status: 'next', items: ['spam reporting','block/report','message requests','rate limiting','moderation queue','audit trail','abuse escalation','safe uploads','malware scanning architecture','appeals','account takeover detection'] },
  { id: 'accessibility', phase: 2, title: 'Accessibility & Globalization', status: 'next', items: ['keyboard navigation','focus management','ARIA','screen reader','contrast','reduced motion','text scaling','captions','RTL readiness','English','Telugu','Hindi','global localization'] },
  { id: 'backup', phase: 2, title: 'Data Portability & Disaster Recovery', status: 'next', items: ['account export','media export','backup strategy','restore strategy','point-in-time recovery','disaster recovery drills','persistent media storage'] },
  { id: 'observability', phase: 1, title: 'Production Observability', status: 'next', items: ['request IDs','message latency','API latency','realtime connection health','reconnect rate','failed-message rate','search latency','media latency','call setup rate','call drop rate','database latency','crash/error rate','privacy-safe logs'] }
] as const;

export async function registerProductCenterRoutes(app: FastifyInstance, prisma: PrismaClient) {
  const auth = secured(app);
  app.get('/api/product/features', auth, async () => PRODUCT_FEATURES);
  app.get('/api/account/sessions', auth, async request => { const userId = (request.user as AuthUser).id; return prisma.userSession.findMany({ where: { userId, revokedAt: null }, orderBy: { lastSeenAt: 'desc' }, select: { id: true, deviceName: true, platform: true, userAgent: true, ipAddress: true, createdAt: true, lastSeenAt: true, expiresAt: true } }); });
  app.delete<{ Params: { id: string } }>('/api/account/sessions/:id', auth, async (request, reply) => { const userId = (request.user as AuthUser).id; const session = await prisma.userSession.findFirst({ where: { id: request.params.id, userId, revokedAt: null } }); if (!session) return reply.notFound('Session not found'); await prisma.userSession.update({ where: { id: session.id }, data: { revokedAt: new Date() } }); return { ok: true }; });
  app.post('/api/account/sessions/revoke-others', auth, async (request: any) => { const userId = (request.user as AuthUser).id; const currentSessionId = typeof request.body?.currentSessionId === 'string' ? request.body.currentSessionId : null; const result = await prisma.userSession.updateMany({ where: { userId, revokedAt: null, ...(currentSessionId ? { id: { not: currentSessionId } } : {}) }, data: { revokedAt: new Date() } }); return { ok: true, revoked: result.count }; });
  app.get('/api/privacy/settings', auth, async request => { const userId = (request.user as AuthUser).id; return prisma.user.findUnique({ where: { id: userId }, select: { privacyLastSeen: true, privacyProfilePhoto: true } }); });
  app.patch('/api/privacy/settings', auth, async (request, reply) => { const userId = (request.user as AuthUser).id; const parsed = z.object({ privacyLastSeen: z.enum(['everyone','contacts','nobody']).optional(), privacyProfilePhoto: z.enum(['everyone','contacts','nobody']).optional() }).safeParse(request.body ?? {}); if (!parsed.success) return reply.badRequest('Invalid privacy settings'); return prisma.user.update({ where: { id: userId }, data: parsed.data, select: { privacyLastSeen: true, privacyProfilePhoto: true } }); });
  app.get('/api/media', auth, async request => { const userId = (request.user as AuthUser).id; const q = request.query as { conversationId?: string; type?: string; limit?: string }; const limit = Math.min(Math.max(Number(q.limit ?? 100), 1), 300); const memberships = await prisma.conversationMember.findMany({ where: { userId, ...(q.conversationId ? { conversationId: q.conversationId } : {}) }, select: { conversationId: true } }); const ids = memberships.map(x => x.conversationId); if (!ids.length) return []; return prisma.message.findMany({ where: { conversationId: { in: ids }, attachmentUrl: { not: null }, ...(q.type ? { attachmentMime: { startsWith: q.type } } : {}) }, orderBy: { createdAt: 'desc' }, take: limit, select: { id: true, conversationId: true, senderId: true, body: true, type: true, attachmentUrl: true, attachmentName: true, attachmentMime: true, attachmentSize: true, createdAt: true } }); });
  app.get('/api/media/export', auth, async request => { const userId = (request.user as AuthUser).id; const memberships = await prisma.conversationMember.findMany({ where: { userId }, select: { conversationId: true } }); const ids = memberships.map(x => x.conversationId); const items = ids.length ? await prisma.message.findMany({ where: { conversationId: { in: ids }, attachmentUrl: { not: null } }, orderBy: { createdAt: 'desc' }, take: 5000, select: { id: true, conversationId: true, senderId: true, body: true, type: true, attachmentUrl: true, attachmentName: true, attachmentMime: true, attachmentSize: true, createdAt: true } }) : []; return { ok: true, exportedAt: new Date().toISOString(), count: items.length, items }; });
  app.get('/api/devices', auth, async request => { const userId = (request.user as AuthUser).id; return prisma.userSession.findMany({ where: { userId, revokedAt: null }, orderBy: { lastSeenAt: 'desc' }, select: { id: true, deviceName: true, platform: true, userAgent: true, ipAddress: true, createdAt: true, lastSeenAt: true, expiresAt: true } }); });
  app.post('/api/devices', auth, async (request, reply) => { const userId = (request.user as AuthUser).id; const parsed = z.object({ token: z.string().min(8).max(4096), platform: z.string().min(1).max(64) }).safeParse(request.body ?? {}); if (!parsed.success) return reply.badRequest('Invalid device registration'); return { ok: true, registered: true, userId, platform: parsed.data.platform }; });
}
