let ctx:AudioContext|null=null;let ringtoneTimer:number|undefined;function audio(){if(!ctx)ctx=new AudioContext();if(ctx.state==='suspended')ctx.resume();return ctx}function tone(freq:number,duration=.12,volume=.035){try{const c=audio(),o=c.createOscillator(),g=c.createGain();o.frequency.value=freq;o.type='sine';g.gain.setValueAtTime(volume,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+duration);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+duration)}catch{}}export function messagePing(){tone(880,.09);setTimeout(()=>tone(1175,.12),80)}export function typingTick(){tone(520,.035,.018)}export function stopRingtone(){if(ringtoneTimer)window.clearInterval(ringtoneTimer);ringtoneTimer=undefined}export function startRingtone(video=false){stopRingtone();const play=()=>{tone(video?660:540,.25,.055);setTimeout(()=>tone(video?880:680,.3,.055),280)};play();ringtoneTimer=window.setInterval(play,1800)}