import fs from 'node:fs';
import path from 'node:path';

const file = path.resolve(process.cwd(), 'src/features.ts');
if (!fs.existsSync(file)) process.exit(0);

let source = fs.readFileSync(file, 'utf8');
const oldValue = "const API = import.meta.env.VITE_API_URL ?? 'http://localhost:4000';";
const newValue = "const API = (window as any).__GM_CONFIG__?.API_URL || import.meta.env.VITE_API_URL || (import.meta.env.DEV ? window.location.origin : 'https://global-messanger-backend.onrender.com');";
if (source.includes(oldValue)) source = source.replace(oldValue, newValue);

// Calls: ring the caller only while the recipient is online, keep the calling screen
// open while waiting, and start the elapsed timer only after the call is answered.
if (!source.includes('gm-call-behavior-v3')) {
  source = source.replace(
    "function renderCall(title: string, showLocal: boolean) {",
    "// gm-call-behavior-v3\nlet gmOutgoingPresenceTimer: number | undefined;\nfunction isCallPeerOnline(c: any) { const me = JSON.parse(localStorage.getItem('gm_user') || '{}'); const member = c?.members?.find((m: any) => String(m.user?.id) !== String(me.id)); const uid = member?.user?.id; const presence = (window as any).__gmPresence || {}; return uid ? Boolean(presence[String(uid)]) : false; }\nfunction setConnectedTitle(title: string) { const head = document.querySelector('#gm-feature-overlay b'); if (head) head.textContent = title; }\nfunction stopCallPresenceWatch() { if (gmOutgoingPresenceTimer !== undefined) window.clearInterval(gmOutgoingPresenceTimer); gmOutgoingPresenceTimer = undefined; stopRingtone(); }\nfunction watchCallPeerPresence(c: any, video: boolean) { stopCallPresenceWatch(); const sync = () => { if (!document.getElementById('gm-feature-overlay')) { stopCallPresenceWatch(); return; } if (isCallPeerOnline(c)) startRingtone(video); else stopRingtone(); }; sync(); gmOutgoingPresenceTimer = window.setInterval(sync, 700); }\n\nfunction renderCall(title: string, showLocal: boolean) {"
  );
  source = source.replace("localPreview(); startTimer(); }", "localPreview(); }");
  source = source.replace(
    "renderCall(`Calling ${chatLabel()}`, kind === 'video'); hideSignalMessages();",
    "renderCall(`Calling ${chatLabel()}`, kind === 'video'); hideSignalMessages(); watchCallPeerPresence(c, kind === 'video');"
  );
  source = source.replace(
    "async function answer(d: any) { if (pc) { await pc.setRemoteDescription(d.answer);",
    "async function answer(d: any) { if (pc) { stopCallPresenceWatch(); setConnectedTitle(`Connected with ${chatLabel()}`); startTimer(); await pc.setRemoteDescription(d.answer);"
  );
  source = source.replace(
    "renderCall(`Connected with ${esc(d.fromName || 'Contact')}`, d.callKind === 'video'); hideSignalMessages();",
    "renderCall(`Connected with ${esc(d.fromName || 'Contact')}`, d.callKind === 'video'); startTimer(); hideSignalMessages();"
  );
}

// Premium UI layer: modern messaging layout, stronger hierarchy, softer surfaces,
// touch-friendly controls, polished mobile navigation and accessible focus states.
if (!source.includes('gm-premium-ui-v1')) {
  const premiumUi = `\n// gm-premium-ui-v1\nfunction injectPremiumUI(){\n  if(document.getElementById('gm-premium-ui')) return;\n  const style=document.createElement('style'); style.id='gm-premium-ui';\n  style.textContent=\`\n    :root{--gm-primary:#5b5ce2;--gm-primary-2:#7c5cff;--gm-ink:#182033;--gm-muted:#7b8498;--gm-line:#e9ebf3;--gm-surface:#f7f8fc;--gm-card:#fff;--gm-shadow:0 14px 40px rgba(24,32,51,.08)}\n    html,body,#root{background:var(--gm-surface)} body{color:var(--gm-ink)}\n    .shell{background:#f6f7fb;gap:0}\n    .sidebar{width:350px;background:rgba(255,255,255,.96);border-right:1px solid var(--gm-line);box-shadow:8px 0 30px rgba(24,32,51,.025)}\n    .brand{height:78px;padding:0 22px;border-bottom:1px solid #f0f1f6}\n    .brand-icon{width:42px;height:42px;border-radius:13px;background:linear-gradient(135deg,var(--gm-primary),var(--gm-primary-2));box-shadow:0 8px 18px rgba(91,92,226,.22)}\n    .brand b{font-size:18px;letter-spacing:-.02em}.brand span{font-size:11px;letter-spacing:.02em}\n    .profile{margin:12px 12px 8px;padding:12px;border:1px solid #edf0f6;border-radius:16px;background:#fbfcff}\n    .profile:hover{border-color:#e1e4f1;box-shadow:var(--gm-shadow)}\n    .avatar{box-shadow:inset 0 0 0 1px rgba(255,255,255,.18)}\n    .search{height:46px;margin:4px 16px 13px;border:1px solid #e6e8f0;background:#f5f6fa;border-radius:14px;transition:.18s}\n    .search:focus-within{background:#fff;border-color:#cfd3ee;box-shadow:0 0 0 4px rgba(91,92,226,.08)}\n    .nav{padding:0 12px 12px;grid-template-columns:1fr 1fr;gap:7px}.nav button{height:42px;border-radius:12px;justify-content:center}.nav button.active{background:linear-gradient(135deg,#eeeefe,#e9eaff);color:#5557d9}\n    .section-title{padding:12px 21px 7px;color:#a0a6b6;font-size:9px}\n    .chat-list{padding:3px 9px}.chat-item{padding:11px 10px;border:1px solid transparent;transition:.16s}.chat-item:hover{background:#f7f8fc}.chat-item.selected{background:#eef0ff;border-color:#e0e2fa;box-shadow:0 5px 18px rgba(91,92,226,.06)}\n    .chat-copy b{font-size:13px;color:#263047}.chat-copy p{font-size:11px;color:#8a92a3}\n    .conversation{background:#fff}.topbar{height:78px;padding:0 25px;background:rgba(255,255,255,.95);border-bottom:1px solid var(--gm-line);box-shadow:0 2px 14px rgba(24,32,51,.025)}\n    .chat-heading .avatar{width:43px;height:43px}.chat-heading b{font-size:15px;letter-spacing:-.01em}.chat-heading span{color:#8991a3}.top-actions .icon-btn{border-radius:12px}.top-actions .icon-btn:hover{background:#eef0ff;color:#5557d9}\n    .messages{padding:28px max(6%,80px);background:radial-gradient(circle at 50% 0,#fbfcff 0,#fff 45%);scrollbar-width:thin}\n    .day span{background:#f3f4f8;border:1px solid #eceef4;color:#8b93a3}\n    .bubble{max-width:min(65%,560px);padding:11px 14px;border-radius:17px 17px 17px 6px;background:#f1f3f8;box-shadow:0 2px 8px rgba(24,32,51,.025)}\n    .own .bubble{background:linear-gradient(135deg,var(--gm-primary),#6869ee);border-radius:17px 17px 6px 17px;box-shadow:0 7px 18px rgba(91,92,226,.16)}\n    .bubble p{font-size:13px}.bubble small{font-size:9px}.bubble-menu{width:30px;height:30px;border:1px solid #edf0f5}\n    .composer-wrap{padding:12px max(6%,80px) 20px;background:#fff;border-top:1px solid #f0f1f5}.composer{height:56px;border:1px solid #dfe3ed;border-radius:18px;padding:0 8px 0 6px;box-shadow:0 7px 28px rgba(24,32,51,.07);transition:.18s}.composer:focus-within{border-color:#c9ccee;box-shadow:0 8px 30px rgba(91,92,226,.11),0 0 0 4px rgba(91,92,226,.05)}.composer input{font-size:13px}.send{width:42px;height:42px;border-radius:13px;background:linear-gradient(135deg,var(--gm-primary),#696af0);box-shadow:0 7px 16px rgba(91,92,226,.2)}\n    .modal-backdrop,#gm-enhance-modal{backdrop-filter:blur(7px)}.modal,.gm-enhance-card{border:1px solid #eceef4;box-shadow:0 30px 90px rgba(24,32,51,.18)}\n    .primary{background:linear-gradient(135deg,var(--gm-primary),#696af0);box-shadow:0 7px 16px rgba(91,92,226,.16)}\n    button:focus-visible,input:focus-visible{outline:3px solid rgba(91,92,226,.2);outline-offset:2px}\n    @media(max-width:760px){\n      .sidebar{width:100%;background:#f8f9fc}.brand{height:68px;padding:0 16px}.profile{margin:8px 10px 7px;padding:10px}.search{margin:4px 12px 10px}.nav{padding:0 10px 9px}.section-title{padding:10px 17px 6px}.chat-list{padding:2px 7px}.chat-item{padding:12px 9px;border-radius:14px}.conversation{background:#fff}.topbar{height:calc(64px + env(safe-area-inset-top));min-height:64px;padding:env(safe-area-inset-top) 8px 0}.messages{padding:15px 10px 10px}.bubble{max-width:88%;padding:10px 13px;border-radius:16px 16px 16px 5px}.own .bubble{border-radius:16px 16px 5px 16px}.composer-wrap{padding:8px 9px calc(9px + env(safe-area-inset-bottom));box-shadow:0 -4px 18px rgba(24,32,51,.04)}.composer{min-height:52px;border-radius:17px}.composer input{font-size:16px}.top-actions .icon-btn{width:40px;height:40px}.back-btn{width:40px;height:40px}\n    }\n    @media(min-width:761px){.chat-list::-webkit-scrollbar,.messages::-webkit-scrollbar{width:7px}.chat-list::-webkit-scrollbar-thumb,.messages::-webkit-scrollbar-thumb{background:#d9ddea;border-radius:10px}}\n  \`; document.head.appendChild(style);\n}\n`;
  source = source.replace("function App(){", premiumUi + "\nfunction App(){injectPremiumUI();");
}

fs.writeFileSync(file, source);\n