(() => {
  const KEY='gm_completion_v2';
  const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return {}}};
  const write=v=>localStorage.setItem(KEY,JSON.stringify(v));
  const base=()=>window.__GM_CONFIG__?.API_URL||location.origin;
  const tok=()=>localStorage.getItem('gm_token')||'';
  const req=async(path,options={})=>{const r=await fetch(base()+path,{...options,headers:{'Content-Type':'application/json',...(tok()?{Authorization:`Bearer ${tok()}`}:{}) ,...(options.headers||{})}});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={message:t}}if(!r.ok)throw Error(d.message||`Request failed (${r.status})`);return d};
  const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const status=x=>{const n=document.querySelector('#gm-ac-status');if(n)n.textContent=x};
  const css=`#gm-ac{position:fixed;inset:0;z-index:40000;background:rgba(0,0,0,.62);backdrop-filter:blur(8px);display:flex;align-items:center;justify-content:center;font-family:system-ui,sans-serif;color:#111}#gm-ac .box{width:min(1120px,95vw);height:min(800px,94vh);background:#fff;border-radius:22px;box-shadow:0 30px 100px rgba(0,0,0,.4);display:grid;grid-template-columns:220px 1fr;overflow:hidden}#gm-ac nav{padding:16px;background:#111827;color:#fff;overflow:auto}#gm-ac nav h2{font-size:17px;margin:0 0 14px}#gm-ac nav button{display:block;width:100%;text-align:left;border:0;background:transparent;color:#d1d5db;padding:9px;border-radius:9px;cursor:pointer;margin:2px 0}#gm-ac nav button.active,#gm-ac nav button:hover{background:#374151;color:#fff}#gm-ac main{padding:22px;overflow:auto}#gm-ac header{display:flex;justify-content:space-between;align-items:start}#gm-ac h1{margin:0 0 4px;font-size:23px}.gmgrid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:12px}.gmcard{border:1px solid #e5e7eb;border-radius:14px;padding:14px}.gmcard h3{margin:0 0 7px;font-size:15px}.gmcard button,.gmcard input,.gmcard select,.gmcard textarea{margin-top:7px}.gmcard button{border:1px solid #d1d5db;background:#fff;border-radius:8px;padding:8px 10px;cursor:pointer}.primary{background:#111827!important;color:#fff!important}.danger{color:#b91c1c}.gmstatus{padding:9px;background:#f3f4f6;border-radius:9px;margin:9px 0}.gmlist{max-height:300px;overflow:auto}.gmitem{padding:8px;border-bottom:1px solid #eee}.muted{font-size:13px;color:#6b7280}.preview{max-width:100%;max-height:390px;border-radius:10px;margin-top:10px}@media(max-width:720px){#gm-ac .box{grid-template-columns:1fr;height:96vh}#gm-ac nav{display:flex;gap:4px;padding:7px}#gm-ac nav h2{display:none}#gm-ac nav button{width:auto;white-space:nowrap}}
`;
  function open(){if(document.getElementById('gm-ac'))return;const st=document.createElement('style');st.textContent=css;document.head.appendChild(st);const r=document.createElement('div');r.id='gm-ac';r.innerHTML=`<div class="box"><nav><h2>Advanced Center</h2><button data-tab="overview" class="active">Overview</button><button data-tab="media">Media</button><button data-tab="calls">Calls</button><button data-tab="notifications">Notifications</button><button data-tab="organization">Folders & Saved</button><button data-tab="security">Security</button><button data-tab="ai">AI</button><button data-tab="reliability">Reliability</button><button data-tab="appearance">Appearance</button></nav><main><header><div><h1 id="gm-ac-title">Completion controls</h1><div class="muted">Real browser/server controls are usable here; native/production-only checks are not falsely marked complete.</div></div><button id="gm-ac-close">×</button></header><div id="gm-ac-body"></div></main></div>`;document.body.appendChild(r);r.querySelector('#gm-ac-close').onclick=()=>r.remove();r.addEventListener('click',e=>{const b=e.target.closest('[data-tab]');if(!b)return;r.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.tab)});render('overview');}
  function render(tab){const b=document.querySelector('#gm-ac-body');if(!b)return;document.querySelector('#gm-ac-title').textContent=tab.replace(/(^|\s)\S/g,m=>m.toUpperCase());const d=read();
    if(tab==='overview'){b.innerHTML=`<div id="gm-ac-status" class="gmstatus">Ready.</div><div class="gmgrid"><div class="gmcard"><h3>Voice notes</h3><p class="muted">Record and preview audio locally.</p><button class="primary" id="rec">Start recording</button><div id="recout"></div></div><div class="gmcard"><h3>Image compression</h3><input id="img" type="file" accept="image/*"><button id="compress">Compress</button><div id="imgout"></div></div><div class="gmcard"><h3>App PIN / screen lock</h3><p class="muted">Web Crypto PIN with foreground re-lock.</p><button id="pin">${d.pinHash?'Change/disable PIN':'Set PIN'}</button></div><div class="gmcard"><h3>Storage / cache</h3><p id="storage">Calculating…</p><button id="cache">Clear browser cache</button></div></div>`;voice();document.querySelector('#compress').onclick=compress;document.querySelector('#pin').onclick=pin;storage();document.querySelector('#cache').onclick=clearCache;return}
    if(tab==='media'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Complete media viewer</h3><input id="file" type="file" accept="image/*,video/*,audio/*,.pdf,.txt,.json,.csv"><div id="view"></div></div><div class="gmcard"><h3>Upload progress / cancellation</h3><input id="upload" type="file"><button id="up">Upload</button><div id="progress" class="gmstatus">0%</div><button id="cancel">Cancel</button></div><div class="gmcard"><h3>Link preview</h3><input id="link" placeholder="https://example.com"><button id="preview">Preview</button><div id="linkout" class="muted"></div></div></div>`;document.querySelector('#file').onchange=viewer;document.querySelector('#up').onclick=upload;document.querySelector('#cancel').onclick=()=>window.__gmUploadController?.abort();document.querySelector('#preview').onclick=()=>{try{const u=new URL(document.querySelector('#link').value.trim());if(!/^https?:$/.test(u.protocol))throw Error();document.querySelector('#linkout').innerHTML=`<strong>${esc(u.hostname)}</strong><br>${esc(u.href)}`}catch{status('Enter a valid http(s) URL.')}};return}
    if(tab==='calls'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Speaker / permission / screen</h3><button id="perm">Check camera + microphone</button><button id="speaker">Audio output capability</button><button id="screen">Start screen share</button><video id="sv" class="preview" autoplay muted playsinline hidden></video></div><div class="gmcard"><h3>Call recovery</h3><button id="net">Network capability check</button><p class="muted">Group calling and real TURN relay still require the production call engine and TURN service.</p><div id="netout"></div></div></div>`;document.querySelector('#perm').onclick=async()=>{try{const s=await navigator.mediaDevices.getUserMedia({audio:true,video:true});s.getTracks().forEach(t=>t.stop());status('Camera and microphone permission granted.')}catch(e){status('Permission denied/unavailable: '+(e.message||'unknown'))}};document.querySelector('#speaker').onclick=()=>status('setSinkId: '+(typeof HTMLMediaElement.prototype.setSinkId==='function'?'supported':'unsupported'));document.querySelector('#screen').onclick=async()=>{try{const s=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});const v=document.querySelector('#sv');v.hidden=false;v.srcObject=s;s.getVideoTracks()[0].onended=()=>v.hidden=true;status('Display stream acquired; active call must attach the track.')}catch(e){status(e.message||'Screen capture denied')}};document.querySelector('#net').onclick=()=>document.querySelector('#netout').textContent=`online=${navigator.onLine}, connection=${navigator.connection?.effectiveType||'unknown'}`;return}
    if(tab==='notifications'){b.innerHTML=`<div class="gmcard"><h3>Notification history & sounds</h3><select id="sound"><option>default</option><option>soft</option><option>silent</option></select><button id="saveSound">Save sound</button><button id="notify">Permission</button><div class="gmlist" id="history"></div></div>`;document.querySelector('#sound').value=d.sound||'default';document.querySelector('#saveSound').onclick=()=>{const x=read();x.sound=document.querySelector('#sound').value;write(x);status('Sound preference saved.')};document.querySelector('#notify').onclick=async()=>{if('Notification'in window)status('Permission: '+await Notification.requestPermission())};document.querySelector('#history').innerHTML=(d.notifications||[]).slice(-100).reverse().map(x=>`<div class="gmitem">${esc(x.title)} — ${esc(x.body)}<br><small>${new Date(x.at).toLocaleString()}</small></div>`).join('')||'<p class="muted">No local notification history.</p>';return}
    if(tab==='organization'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Custom folders</h3><input id="folder" placeholder="Folder name"><button id="addFolder">Add</button><div id="folders"></div></div><div class="gmcard"><h3>Saved Messages</h3><button id="saved">Refresh</button><div id="savedList" class="gmlist"></div></div></div>`;document.querySelector('#folders').innerHTML=(d.folders||[]).map((x,i)=>`<div class="gmitem">${esc(x)} <button data-del="${i}">×</button></div>`).join('');document.querySelector('#addFolder').onclick=()=>{const v=document.querySelector('#folder').value.trim();if(!v)return;const x=read();x.folders=[...(x.folders||[]),v].slice(0,50);write(x);render(tab)};document.querySelector('#folders').onclick=e=>{const i=e.target.closest('[data-del]');if(!i)return;const x=read();x.folders.splice(Number(i.dataset.del),1);write(x);render(tab)};document.querySelector('#saved').onclick=async()=>{try{const r=await req('/api/messages/saved');document.querySelector('#savedList').innerHTML=(Array.isArray(r)?r:[]).map(x=>`<div class="gmitem">${esc(x.message?.body||'Saved message')}</div>`).join('')||'<p class="muted">Nothing saved.</p>'}catch(e){status(e.message)}};return}
    if(tab==='security'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Password</h3><button id="change">Change password</button><button id="reset">Request reset</button></div><div class="gmcard"><h3>Recovery / passkeys</h3><button id="security">Check security APIs</button><p class="muted">Recovery codes are server-backed with 2FA.</p></div><div class="gmcard"><h3>Privacy</h3><button id="privacy">Load privacy</button><pre id="privacyOut"></pre></div><div class="gmcard"><h3>Delete account</h3><button class="danger" id="delete">Delete permanently</button></div></div>`;document.querySelector('#change').onclick=async()=>{const c=prompt('Current password');const n=c&&prompt('New password');if(c&&n)try{await req('/api/auth/change-password',{method:'POST',body:JSON.stringify({currentPassword:c,newPassword:n})});status('Password changed.')}catch(e){status(e.message)}};document.querySelector('#reset').onclick=()=>{const email=prompt('Account email');if(email)req('/api/auth/forgot-password',{method:'POST',body:JSON.stringify({email})}).then(()=>status('If the account exists, reset instructions were requested.')).catch(e=>status(e.message))};document.querySelector('#security').onclick=()=>req('/api/security/passkeys').then(x=>status(`Security API reachable; ${Array.isArray(x)?x.length:0} passkeys.`)).catch(e=>status(e.message));document.querySelector('#privacy').onclick=()=>req('/api/privacy/settings').then(x=>document.querySelector('#privacyOut').textContent=JSON.stringify(x,null,2)).catch(e=>status(e.message));document.querySelector('#delete').onclick=async()=>{if(!confirm('Permanently delete this account?'))return;const p=prompt('Current password');if(!p)return;try{await req('/api/auth/account',{method:'DELETE',body:JSON.stringify({password:p})});localStorage.clear();location.reload()}catch(e){status(e.message)}};return}
    if(tab==='ai'){b.innerHTML=`<div class="gmcard"><h3>AI rewrite / translation / reply / search</h3><textarea id="aiText" rows="6" style="width:100%" placeholder="Message or search context"></textarea><div><button data-ai="rewrite">Rewrite</button><button data-ai="translate">Translate</button><button data-ai="reply suggestion">Reply</button><button data-ai="search messages">AI search</button></div><pre id="aiOut" class="gmstatus"></pre><h3>Voice transcription</h3><button id="speech">Start browser transcription</button></div>`;b.querySelectorAll('[data-ai]').forEach(x=>x.onclick=async()=>{const text=document.querySelector('#aiText').value.trim();if(!text)return;try{const r=await req('/api/ai/assist',{method:'POST',body:JSON.stringify({prompt:x.dataset.ai+': '+text,context:text})});document.querySelector('#aiOut').textContent=r.output||r.message||JSON.stringify(r)}catch(e){document.querySelector('#aiOut').textContent=e.message}});document.querySelector('#speech').onclick=transcribe;return}
    if(tab==='reliability'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Draft persistence</h3><textarea id="draft" rows="5" style="width:100%"></textarea><button id="saveDraft">Save</button><button id="loadDraft">Restore</button></div><div class="gmcard"><h3>API availability</h3><p>${navigator.onLine?'Online':'Offline'}</p><button id="ping">Ping API</button><button id="clearOutbox">Clear local outbox</button></div><div class="gmcard"><h3>Large file</h3><input id="large" type="file"><div id="largeOut" class="muted"></div></div></div>`;document.querySelector('#saveDraft').onclick=()=>{const x=read();x.draft=document.querySelector('#draft').value;write(x);status('Draft persisted.')};document.querySelector('#loadDraft').onclick=()=>document.querySelector('#draft').value=read().draft||'';document.querySelector('#ping').onclick=async()=>{try{await req('/api/health');status('API reachable.')}catch(e){status('API unavailable: '+e.message)}};document.querySelector('#clearOutbox').onclick=()=>{const x=read();x.outbox=[];write(x);status('Local outbox cleared.')};document.querySelector('#large').onchange=e=>{const f=e.target.files?.[0];document.querySelector('#largeOut').textContent=f?`${f.name}: ${(f.size/1024/1024).toFixed(2)} MB`:'No file'};return}
    if(tab==='appearance'){b.innerHTML=`<div class="gmgrid"><div class="gmcard"><h3>Wallpaper / font size</h3><select id="theme"><option>system</option><option>light</option><option>dark</option></select><select id="font"><option value="14">Small</option><option value="16">Default</option><option value="18">Large</option><option value="20">Extra large</option></select><input id="wall" placeholder="Wallpaper URL"><button id="apply">Apply</button></div><div class="gmcard"><h3>Native validation</h3><p class="muted">Android biometric, push, real TURN, physical audio routing, Wi-Fi/mobile handoff and multi-device E2EE need real devices/services.</p></div></div>`;const x=read();document.querySelector('#theme').value=x.theme||'system';document.querySelector('#font').value=x.font||'16';document.querySelector('#wall').value=x.wall||'';document.querySelector('#apply').onclick=()=>{const s=read();s.theme=document.querySelector('#theme').value;s.font=document.querySelector('#font').value;s.wall=document.querySelector('#wall').value;write(s);document.documentElement.dataset.theme=s.theme;document.documentElement.style.fontSize=s.font+'px';if(s.wall)document.body.style.backgroundImage=`url(${JSON.stringify(s.wall)})`;status('Appearance saved.')};return}
  }
  function voice(){let rec,parts=[];document.querySelector('#rec').onclick=async()=>{if(rec?.state==='recording'){rec.stop();return}try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});parts=[];rec=new MediaRecorder(stream);rec.ondataavailable=e=>e.data.size&&parts.push(e.data);rec.onstop=()=>{stream.getTracks().forEach(t=>t.stop());const blob=new Blob(parts,{type:rec.mimeType||'audio/webm'});const u=URL.createObjectURL(blob);document.querySelector('#recout').innerHTML=`<audio class="preview" controls src="${u}"></audio> <a download="voice-note.webm" href="${u}">Download</a>`;status('Voice note recorded.');};rec.start();status('Recording… click again to stop.')}catch(e){status(e.message||'Microphone unavailable')}}}
  async function compress(){const f=document.querySelector('#img').files?.[0];if(!f)return;const img=new Image();img.onload=()=>{const max=1600,s=Math.min(1,max/img.width,max/img.height),c=document.createElement('canvas');c.width=Math.round(img.width*s);c.height=Math.round(img.height*s);c.getContext('2d').drawImage(img,0,0,c.width,c.height);c.toBlob(blob=>{if(!blob)return;const u=URL.createObjectURL(blob);document.querySelector('#imgout').innerHTML=`${Math.round(blob.size/1024)} KB <a download="compressed-${esc(f.name)}" href="${u}">Download</a>`},'image/jpeg',.78)};img.src=URL.createObjectURL(f)}
  function viewer(e){const f=e.target.files?.[0],v=document.querySelector('#view');if(!f)return;const u=URL.createObjectURL(f);if(f.type.startsWith('image/'))v.innerHTML=`<img class="preview" src="${u}">`;else if(f.type.startsWith('video/'))v.innerHTML=`<video class="preview" controls src="${u}"></video>`;else if(f.type.startsWith('audio/'))v.innerHTML=`<audio class="preview" controls src="${u}"></audio>`;else if(f.type==='application/pdf')v.innerHTML=`<iframe class="preview" style="width:100%;height:390px" src="${u}"></iframe>`;else v.innerHTML=`<a download="${esc(f.name)}" href="${u}">Open/download ${esc(f.name)}</a>`}
  async function upload(){const f=document.querySelector('#upload').files?.[0];if(!f)return;const c=new AbortController();window.__gmUploadController=c;try{await new Promise((resolve,reject)=>{const x=new XMLHttpRequest();x.open('POST',base()+'/api/uploads');if(tok())x.setRequestHeader('Authorization','Bearer '+tok());x.upload.onprogress=e=>{if(e.lengthComputable)document.querySelector('#progress').textContent=Math.round(e.loaded/e.total*100)+'%'};x.onload=()=>x.status>=200&&x.status<300?resolve(x.responseText):reject(Error('Upload failed ('+x.status+')'));x.onerror=()=>reject(Error('Network error'));x.onabort=()=>reject(new DOMException('Upload cancelled','AbortError'));x.send((()=>{const fd=new FormData();fd.append('file',f);return fd})())});status('Upload complete.')}catch(e){status(e.name==='AbortError'?'Upload cancelled.':e.message)}finally{window.__gmUploadController=null}}
  function transcribe(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){status('Browser speech recognition is unavailable; configure a server speech model for uploaded-audio transcription.');return}const r=new SR();r.continuous=true;r.interimResults=true;r.lang=navigator.language||'en-US';r.onresult=e=>{let s='';for(let i=e.resultIndex;i<e.results.length;i++)s+=e.results[i][0].transcript+' ';const o=document.querySelector('#aiOut');if(o)o.textContent=s.trim()};r.onerror=e=>status('Transcription error: '+e.error);r.start();status('Listening…')}
  async function pin(){const x=read();if(x.pinHash&&confirm('Disable current PIN?')){delete x.pinHash;write(x);status('PIN disabled.');return}const p=prompt('Set a 4-12 digit PIN');if(!/^\d{4,12}$/.test(p||''))return;const h=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(p));x.pinHash=Array.from(new Uint8Array(h)).map(n=>n.toString(16).padStart(2,'0')).join('');write(x);status('PIN configured. Re-lock occurs when the app returns to foreground.')}
  async function storage(){try{const q=await navigator.storage?.estimate?.();document.querySelector('#storage').textContent=q?`${(q.usage/1024/1024).toFixed(1)} MB used / ${(q.quota/1024/1024).toFixed(0)} MB`:'Unavailable'}catch{}}
  async function clearCache(){try{for(const k of await caches.keys())await caches.delete(k)}catch{}const x=read();x.outbox=[];write(x);status('Browser cache and local outbox cleared; login token preserved.')}
  document.addEventListener('visibilitychange',async()=>{if(document.visibilityState!=='visible')return;const x=read();if(!x.pinHash||sessionStorage.getItem('gm_pin_ok')==='1')return;const p=prompt('Enter App PIN');if(!p){document.body.innerHTML='<div style="padding:40px;font:18px system-ui">App locked.</div>';return}const h=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(p));const hash=Array.from(new Uint8Array(h)).map(n=>n.toString(16).padStart(2,'0')).join('');if(hash===x.pinHash)sessionStorage.setItem('gm_pin_ok','1');else document.body.innerHTML='<div style="padding:40px;font:18px system-ui">Incorrect PIN — app locked.</div>'});
  window.__gmAdvancedCompletion={open};
})();